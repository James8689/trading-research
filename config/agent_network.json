{
  "backend": "manual",
  "models": {"worker": "gpt-5.6-luna", "frontier": "gpt-6-astra"},
  "max_cycles": 2,
  "max_calls": 12,
  "max_seconds_per_call": 300,
  "max_packet_chars": 20000,
  "max_total_tokens": 120000,
  "automatic_calls_enabled": false,
  "pricing_verified": false,
  "spending_policy": "Pending user budget. Manual packets incur no model calls. Token/call budgets are dispatch limits, not a hard provider dollar cap.",
  "role_tiers": {"scout":"worker", "mechanism":"worker", "data":"worker", "adversary":"worker", "coordinator":"frontier"},
  "context_policy": "Fresh task processes; own role memory and dependency summaries only. Reference evidence by file path/hash; never load all raw data into prompts.",
  "promotion_policy": "review_required pauses for frontier coding-agent work; no automatic simulator execution, holdout unsealing, strategy approval or orders."
}
